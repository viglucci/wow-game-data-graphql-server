# import Zone from 'Zone.graphql'

type Guild {
  id: ID
  name: String
  realm: Realm
}

type MythicRaidLeaderboardEntry {
  guild: Guild
  faction: Faction
  region: String
  rank: Int
}

type MythicRaidLeaderboard {
  zone: Zone
  entries: [MythicRaidLeaderboardEntry]
}
